"""Execution reality gate — final checkpoint before real-world actions."""

from odin_backend.core.execution_gate.gate import ExecutionGate, GateDecision, GateResult

__all__ = ["ExecutionGate", "GateDecision", "GateResult"]
