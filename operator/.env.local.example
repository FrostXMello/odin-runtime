# Backend URL for Next.js rewrites (server-side proxy)
ODIN_BACKEND_URL=http://127.0.0.1:8000

# Client-side poll interval (ms) — optional override in UI settings
NEXT_PUBLIC_POLL_INTERVAL_MS=3000

# WebSocket base (Next rewrites do not proxy WS)
NEXT_PUBLIC_WS_BASE=ws://127.0.0.1:8000/api/v1/ws
