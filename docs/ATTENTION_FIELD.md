# Attention Field

Live attention heatmap visualization integrated with operator intelligence V2.

Trace: `attention_heatmap_updated` → `operator-intelligence:runtime`

Operator page: `/attention-field`

Frontend: `frontend/cognitive_workspace/src/adaptive_os/AdaptiveOSPanels.tsx` — `AttentionFieldV2`, `CognitiveLoadHeatmap`

GPU-safe lazy rendering with adaptive FPS scaling.
