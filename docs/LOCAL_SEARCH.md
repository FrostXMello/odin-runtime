# Local Search (v0.36)

Hybrid semantic search across vector memory, workspace knowledge, and projects.

## Configuration

```env
ODIN_LOCAL_SEARCH_ENABLED=true
ODIN_VECTOR_MEMORY_ENABLED=true
```

## API

- `POST /api/v1/runtime/search`
