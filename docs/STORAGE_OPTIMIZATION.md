# Storage Optimization (v0.36)

Long-term storage scaling via embedding cache, archival, and semantic compaction.

## Configuration

```env
ODIN_STORAGE_OPTIMIZATION_ENABLED=true
```

## APIs

- `GET /api/v1/runtime/storage`
- `POST /api/v1/runtime/storage/optimize`
